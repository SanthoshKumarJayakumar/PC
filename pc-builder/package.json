{
  "name": "aetherforge",
  "version": "1.0.0",
  "private": true,
  "description": "AetherForge — custom PC configuration and e-commerce platform",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client && npm run build -w server",
    "lint": "npm run lint -w server && npm run lint -w client",
    "format": "prettier --write \"**/*.{js,jsx,json,css,md}\"",
    "test": "npm run test -w server",
    "db:up": "docker compose up -d",
    "db:generate": "npm run prisma:generate -w server",
    "db:migrate": "npm run prisma:migrate -w server",
    "db:seed": "npm run prisma:seed -w server",
    "db:studio": "npm run prisma:studio -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.4.2"
  },
  "engines": {
    "node": ">=20"
  }
}
